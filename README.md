# todo service

Small FastAPI service. Run tests with `pytest -q` from the repo root.

- `app/main.py` — endpoints: `GET /health`, `GET /todos`, `POST /todos`, `DELETE /todos/{id}`
- `tests/test_todos.py`
